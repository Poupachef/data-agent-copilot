services:
  mockup:
    build: .
    container_name: waha-mockup
    restart: unless-stopped
    ports:
      - "3001:3000"
    networks:
      - waha-network

networks:
  waha-network:
    driver: bridge

